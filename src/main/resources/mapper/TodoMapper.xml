<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.todo.mapper.TodoMapper">
    <!-- 
    // 1. 할일 목록 조회 (특정 토픽)
    List<Todo> findByTopicId(@Param("topicId") Long topicId);
    // 2. 할일 조회 (특정 할일)
    Todo findByTodoId(@Param("todoId") Long todoId);
    // 3. 할일 생성
    void createTodo(Todo todo);
    // 4. 할일 수정
    void updateTodo(Todo todo);
    // 5. 할일 삭제
    void deleteTodo(@Param("todoId") Long todoId);
    // 6. 할일 완료 상태 변경
    void updateTodoStatus(@Param("todoId") Long todoId, @Param("isDone") Boolean isDone); -->


    <!-- 할일 목록 조회 -->
    <select id="findByTopicId" parameterType="long" resultType="Todo"> SELECT todo_id, topic_id,
        title, content, is_done, quadrant FROM todo WHERE topic_id = #{topicId} </select>

    <!-- 할일 조회 -->
    <select id="findByTodoId" parameterType="long" resultType="Todo"> SELECT * FROM todo WHERE
        todo_id = #{todoId} </select>

    <!-- 할일 생성 -->
    <insert id="createTodo" parameterType="Todo" useGeneratedKeys="true" keyProperty="todoId">
        INSERT INTO todo (topic_id, title, content, is_done, quadrant) VALUES (#{topicId}, #{title},
        #{content}, #{isDone}, #{quadrant}) </insert>

    <!-- 할일 수정 -->
    <update id="updateTodo" parameterType="Todo"> UPDATE todo SET title = #{title}, content =
        #{content}, is_done = #{isDone}, quadrant = #{quadrant} WHERE todo_id = #{todoId} </update>

    <!-- 할일 삭제 -->
    <delete id="deleteTodo" parameterType="long"> DELETE FROM todo WHERE todo_id = #{todoId} </delete>

    <!-- 할일 완료 상태 변경 -->
    <update id="updateTodoStatus" parameterType="map"> UPDATE todo SET is_done = #{isDone} WHERE
        todo_id = #{todoId} </update>
</mapper>